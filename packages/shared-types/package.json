{
  "name": "@nudge/shared-types",
  "version": "0.1.0",
  "description": "Shared DTO definitions for NUDGE — used by web app and future API clients",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  },
  "license": "MIT"
}
